In the "What HomeLens does" grid on the homepage (`src/pages/Index.tsx`), reorder the 5 cards so the Chrome Extension card is the first card instead of the last. Keep the existing styling, animations, and delays intact.